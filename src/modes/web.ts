/**
 * Mode: web — placeholder for Phase 4 implementation.
 *
 * This mode provides tools for storefront/web operations:
 * collections, articles, blogs, pages, script tags, SEO.
 *
 * Tools will be implemented in Phase 4 (TDD).
 */

import type { McpServer } from '@modelcontextprotocol/sdk/server/mcp.js';
import type { SapoClient } from '../client/http.js';
import type { SapoConfig } from '../config.js';

export function registerWebTools(
  _server: McpServer,
  _client: SapoClient,
  _config: SapoConfig,
): void {
  // Phase 4: implement 25+ tools for web mode
  // Tools: list_collections, create_collection, list_articles, create_article,
  //        list_blogs, list_pages, list_script_tags, get_shop, etc.
}
